## 3D层级
CSS语法中有两个特性与3D有关，分别是z-index和tranfrom。这两个特性虽说基础，但能实现的效果很强大。

严格来说这其实算不上3D，其实是利用视觉透视和坐标变化模拟的立体空间感。

屏幕这个二维平面对应三维坐标系的x-y平面,z-index对应的就是z轴，由屏幕到你的这一条线。这意味着在同一个位置可以放置多个元素，我们通过设置元素的z-index大小控制遮挡关系组合出独特的样式。举个浅显的例子来说，相册页面书本打开后最下方是棕色书皮，中间是米白色页面，接着是白色相片纸，最上方是图片。这样的组合复刻了现实的相册。

tranfrom能做到的事情往往更多，并且我觉得通过一次实战就能掌握如何使用。

假设要画一个正方体，就需要6个面，需要一个容器来管理整体的透视并开启3D空间。而这些都需要挂载在DOM节点即html类上。

按照这个说法，html是这么写的。
```html
<!-- 容器 -->
<div class ="pillar"> 
    <div class = "face front"></div>
    <div class = "face back"></div>
    <div class = "face left"></div>
    <div class = "face right"></div>
    <div class = "face top"></div>
    <div class = "face bottom"></div>
</div>
```
于此对应，pillar作为容器需要开启三维空间方便子类定位。
```css
.pillar{
    position: relative;
    height: 42px;
    transform-style: preserve-3d;
    width: 42px;
}
```
最前面的面需要向前凸出
```css
.face.front{
    width: 100%;
    height: 42px;
    tranform: translateZ(9px);
    position: absolute;
}
```
后方的面需要旋转后向后凹，怎么理解这个旋转呢，和高中学的右手定则一样：伸出食指，绕X轴转则指向右方，绕Y轴转则指向上方，大拇指就是旋转路径。
```css
.face.back{
    width: 100%;
    height: 42px;
    tranform:  rotateY(180deg)  translateZ(-9px);
    position: absolute;
}
```
顶面则是以X轴转90度然后往上移动
```css
.face.top{
    width: 100%;
    height: 42px;
    tranform:  rotateX(90deg);
    top: 9px;
    position: absolute;
}
```
侧面是以Y轴旋转90度然后往右移动
```css
.face.top{
    width: 100%;
    height: 42px;
    tranform:  rotateY(90deg);
    right: 9px;
    position: absolute;
}
```
最后呈现出的效果就是这样的。我们可以给这个正方体转一个角度即给pillar类加一个旋转让来看的更清楚一些。我这里让它稍微低一点,往右偏一点；并且加上颜色和文字。
<div style = "display: flex">
<img src="/messages/18/true.webp" alt="演示" style="width:50%"/>
<img src="/messages/18/true2.webp" alt="演示" style="width:50%"/>
</div>
能实现的东西还有很多。翻书就是个适合进阶练手动画和旋转组合的例子。

对子元素设置不同的Y轴旋转角度和动画过渡。

<a href="https://www.bilibili.com/video/BV1Ks4y1Z7d1/?share_source=copy_web&vd_source=aa4072bc298a92b8ef0a2929fb1722c7" target='_blank' rel="noopener noreferrer">纯CSS实现书本翻页效果</a>

为了追求更好的光影，高光等，还可以借助外部库three.js来画。但于此同时,这也会带来更大的性能"花销"。

这个three.js我只是听说过还没用过，据说苹果和特斯拉网页的3D建模是靠这个实现的，非常强大。但学起来应该蛮难的，毕竟要考虑帧率，灯光，相机视角，渲染，性能与显存释放等等。
## 进阶动画
老实说，其实我以前有说过动画，主要是用的是css中的tranition(过渡)，写得多了发现其实动画不仅有其他的书写方式,而且可以以更优美的方式表现。

如果基础不牢固的可以看看这个视频，这个up专精css。

<a href="https://www.bilibili.com/video/BV1SRUYBeEUj/?share_source=copy_web&vd_source=aa4072bc298a92b8ef0a2929fb1722c7" target='_blank' rel="noopener noreferrer">搞懂 CSS 动画与过渡</a>

进阶在哪些部分呢？可以是转用css的动画特性，生成更精细的动画，可以是联动GSAP库的Js代码，还可以联动SVG元素...

对于一些没有交互的元素，我会使用animation特性来写动画，比如我的加载屏动画。

### 描字动画
这个是我从外网抄的，动画分成两个部分，第一部分是把文字边框显现出来，第二部分是将颜色填充。文字是一整个SVG元素，css对此有专门的特性。
```html
<!-- 这是文字SVG -->
<svg width="100%" height="100%" viewBox="0 0 800 200" xmlns="http://www.w3.org/2000/svg">
    <g class="animated-text">
        <path class="cls-1" pathLength="1"
            d='...' />
    </g>
</svg>
```
用到的特性是描边-stroke，就是图形轮廓的那一条线。初始时先将图形隐藏，即透明度为0，描边向下偏移。此时屏幕中什么都没有。这里有个小技巧是使用 pathLength="1" 将SVG长度强制设置为1，这样我们的stroke-dasharry与stroke-dashoffset就不用去借助js语句求精确的长度。
```css
.initial-loader .animated-text {
    stroke: #2c2c2c;
    stroke-width：14px;
    fill: transparent;
    stroke-dasharry: 1;  // 使用虚线的效果
    stroke-dashoffset: 1;
}
```
我们使用虚线的效果，并且将长度设置为1是为了形成一段空白和一段实线描边，借助stroke-dashoffset把实现往起点对应的数值，这样初始没有描边。如果我将偏移设置为一般的话，就会出现下面的情况，后半部分的描边到了前半部分，前半部分的描边看不到了。
<img src="/messages/18/true3.webp" alt="演示"/>

接着是定义一个专属动画
```css
@keyframes writetext{
    0%{
        fill: transparent;
        stroke-dashoffset: 1;
    }
    75%{
        fill: transparent;
        stroke-dashoffset: 0;
    }
    100%{
        fill: black;
        stroke-dashoffset: 0;
    }
}
```
接着应用这个动画
```css
.initial-loader .animated-text {
    ... //略
    animation: writetext 3s esse forwards; //停在最后一帧
}
```
### 飞机尾焰
这个很简单，稍微进阶的地方在于要控制尾焰的方向，这就不得不联动起Js代码。

首先我们需要两个html节点，一个来当外焰，一个来当內焰。
```html
<div class="plume-outer" ></div>
<div class="plume-inner" ></div>
```
接着画这个火焰，需要用到css的clip-path + linear-gradient + filter三个特性。

clip-path翻译为剪裁路径，我们用至少三个点连成一个闭合图形，图形外的内容将直接被舍弃
```css
clip-path: polygon(50% 0%, 100% 100%, 0% 100%); //三角形
```
<img src="/messages/18/true4.webp" alt="演示"/>

你可以在下面这个网站获取想要图形的对应clip-path代码

<a href="https://www.jiangweishan.com/tool/clippy/" target='_blank' rel="noopener noreferrer">clippy</a>

linear-gradient翻译是线性渐变，这里用来模拟火焰效果，用法和keyframes差不多,第一个参数是方向，后面的参数是变色节点
```css
background: linear-gradient(
    to bottom，
    white 0%,
    blue 50%,
    red 100% 
)
```
<img src="/messages/18/true5.webp" alt="演示"/>

filter翻译是滤镜，这个有点玄乎，我搞不大懂
<table style="border-collapse: collapse; width: 100%; font-size: 13px;" border="1">
  <thead>
    <tr style="background-color: #f1f5f9;">
      <th style="border: 1px solid #475569; padding: 6px;">滤镜写法</th>
      <th style="border: 1px solid #475569; padding: 6px;">效果</th>
      <th style="border: 1px solid #475569; padding: 6px;">场景</th>
    </tr>
  </thead>
  <tbody>
    <tr><td style="border: 1px solid #475569; padding: 4px 6px;"><code>blur(5px)</code></td><td style="border: 1px solid #475569; padding: 4px 6px;">高斯模糊</td><td style="border: 1px solid #475569; padding: 4px 6px;">尾焰羽化、毛玻璃</td></tr>
    <tr><td style="border: 1px solid #475569; padding: 4px 6px;"><code>drop-shadow(0 0 8px #00f)</code></td><td style="border: 1px solid #475569; padding: 4px 6px;">轮廓发光</td><td style="border: 1px solid #475569; padding: 4px 6px;">配合 clip-path 做霓虹灯</td></tr>
    <tr><td style="border: 1px solid #475569; padding: 4px 6px;"><code>brightness(1.5)</code></td><td style="border: 1px solid #475569; padding: 4px 6px;">提亮爆闪</td><td style="border: 1px solid #475569; padding: 4px 6px;">悬停通电发光</td></tr>
    <tr><td style="border: 1px solid #475569; padding: 4px 6px;"><code>contrast(200%)</code></td><td style="border: 1px solid #475569; padding: 4px 6px;">拉大反差</td><td style="border: 1px solid #475569; padding: 4px 6px;">强化冲击感</td></tr>
    <tr><td style="border: 1px solid #475569; padding: 4px 6px;"><code>hue-rotate(180deg)</code></td><td style="border: 1px solid #475569; padding: 4px 6px;">色相偏移</td><td style="border: 1px solid #475569; padding: 4px 6px;">一键换肤 (蓝火变红火)</td></tr>
    <tr><td style="border: 1px solid #475569; padding: 4px 6px;"><code>saturate(200%)</code></td><td style="border: 1px solid #475569; padding: 4px 6px;">色彩鲜艳</td><td style="border: 1px solid #475569; padding: 4px 6px;">充能、激活</td></tr>
    <tr><td style="border: 1px solid #475569; padding: 4px 6px;"><code>grayscale(100%)</code></td><td style="border: 1px solid #475569; padding: 4px 6px;">全变黑白</td><td style="border: 1px solid #475569; padding: 4px 6px;">未解锁、全站置灰</td></tr>
    <tr><td style="border: 1px solid #475569; padding: 4px 6px;"><code>invert(100%)</code></td><td style="border: 1px solid #475569; padding: 4px 6px;">色彩反相</td><td style="border: 1px solid #475569; padding: 4px 6px;">暗黑模式、扫描反色</td></tr>
    <tr><td style="border: 1px solid #475569; padding: 4px 6px;"><code>opacity(50%)</code></td><td style="border: 1px solid #475569; padding: 4px 6px;">半透明</td><td style="border: 1px solid #475569; padding: 4px 6px;">残影、禁用淡出</td></tr>
    <tr><td style="border: 1px solid #475569; padding: 4px 6px;"><code>sepia(80%)</code></td><td style="border: 1px solid #475569; padding: 4px 6px;">复古黄褐</td><td style="border: 1px solid #475569; padding: 4px 6px;">怀旧胶片风格</td></tr>
  </tbody>
</table>
其中比较重要的是同股票filter: grayscale(100%) opacity(0.6)来展示元素禁用的效果，这个外焰焰的滤镜是高斯模糊，产生气流效果，內焰的滤镜是轮廓发光，强烈的视觉效果。

Js代码用到了反正切函数与跨0度插值算法用于旋转角度，但这不是很重要
```javascript
const dx = mouseX - prevX; // 水平方向位移
const dy = mouseY - prevY; // 垂直方向位移

// 角度计算
targetAngle = (Math.atan2(dy, dx) * 180) / Math.PI + 90;

let diff = (targetAngle - shipAngle) % 360;
// 角度差大于 180 度，往反方向拐
if (diff > 180) diff -= 360;
if (diff < -180) diff += 360;

// 转角度差的20%
const turnRate = diff * 0.20;
shipAngle += turnRate;
```

重点是html绑定js,在html中通过ref定义一个可响应元素
```html
<div ref="shipRef" class="ship-wrapper">
```
再在js中拿到这个元素，设为null是预防一开始这个DOM没有加载出来。拿到后并操控这个DOM对应的css样式。
```javascript
const shipRef = ref(null)

shipRef.value.style.transform = `translate3d(...) rotate(...)`
```
其中用translate3d来控制移动，rotate来控制旋转。